#!/bin/sh
MESSAGE="$(cat "$1")"

echo "🔍 커밋 메시지 검증 중..."

if ! pnpm commitlint --edit "$1" >/dev/null 2>&1; then
  echo "=========================================================================="
  echo "🚨 커밋 메시지 오류가 발생했어요"
  echo "허용된 타입: Feat, Fix, Refactor, Chore, Test, Docs, Style"
  echo "예시: Feat: 카드 추가"
  echo "=========================================================================="
  echo "작성하신 메시지 : $MESSAGE"
  echo "=========================================================================="
  exit 1
fi

echo "=========================================================================="
echo "✅ 커밋이 성공적으로 완료되었어요"
echo "=========================================================================="
